pipeline {
	agent any

	stages {
		stage ('build') {
			steps {
				sh 'echo Compile'
			}
		}
	}

	stages {
		stage ('test') {
			steps {
				sh 'echo Test'
			}
		}
	}

	stages {
		stage ('deploy') {
			steps {
				sh 'echo Deploy'
			}
		}
	}
}

